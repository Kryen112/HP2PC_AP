# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/

# UnrealScript build artifacts
*.u
*.tmp
*.log

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# AP generation
output/
dist-temp/
*.archipelago
tmp_seed_check*/

# PyInstaller client-exe build artifacts.
# The 85 MB onefile exe ships in the release zip, not in git.
dist/

# Reference checkouts (kept outside repo)
/uscript/
/wiki-clone/

# Asset generators kept out of the repo (the logo SVG is committed as the
# source of record; the PSK/PSA + texture generator scripts and their raster
# work files are not -- the baked outputs under mod/HPArchipelago are).
tools/*.py
tools/*.png

# Local configs
.env
*.local.yaml

# containersanity census draft (intermediate; folded into data/locations.yaml)
tools/containers_census_out.yaml
/plans/
